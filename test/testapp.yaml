apiVersion: v1
kind: Pod
metadata:
  name: test-webapp
spec:
  containers:
    - name: webapptest
      image: webapptest:latest
      imagePullPolicy: Never
      ports:
        - containerPort: 80

